# Terraform local state, plans, and variable files can contain identifiers or sensitive values.
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/*.tfplan
terraform/**/terraform.tfvars
terraform/**/crash.log
